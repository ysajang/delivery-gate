# 스캐너가 못 잡는 수동 확인 항목 (납품 전 필수)

자동 게이트를 통과해도 아래는 남는다. 인가 결함은 OWASP 1순위이면서
스캐너가 가장 못 잡는 부류다. 앱마다 정답이 달라 룰로 표현이 안 되기 때문.

## A. 인가 / IDOR
- [ ] 레코드 조회 API에 "호출자가 이 레코드의 주인인가" 검사가 있는가
- [ ] URL의 id를 남의 id로 바꿔 호출 -> 200이 나오면 실패
- [ ] 로그인 없이 API 직접 호출 -> 401/403이 아니면 실패
- [ ] 일반 계정 토큰으로 관리자 API 호출 -> 200이면 실패
- [ ] 권한 검사가 프론트에만 있는가 (JS 수정으로 우회되면 실패)

## B. 노출면
- [ ] .env / 백업 / .git 이 웹으로 접근되는가 (curl로 직접 확인)
- [ ] 관리자 화면이 인터넷에 그대로 열려 있는가
- [ ] 에러 응답에 스택트레이스·쿼리문이 나오는가

## C. 데이터
- [ ] 개발/테스트 DB에 실제 고객 개인정보가 들어있는가
- [ ] 업로드 파일 확장자·용량 검사가 있는가

## D. 비즈니스 로직 (스캐너 원천 불가)
- [ ] 수량 음수 주문, 쿠폰 중복 적용, 결제 건너뛴 주문 생성이 되는가

## E. 인수인계 문서
- [ ] 점검한 범위와 점검하지 않은 범위를 명시했는가
- [ ] 인프라 이관 후 취약점 책임 경계를 적었는가

## F. 외부 의존과 구조적 한계
- [ ] 제3자 로그인 세션·비공식 API·크롤링에 의존하는 기능이 있는가 -> 있으면 만료 시 증상, 복구 절차, 고객이 직접 할 수 있는 조치를 문서에 적고 계약 전에 고지했는가
- [ ] 공식 API 신청이 거절된 경로를 우회했다면 그 사실과 지속 위험을 인수인계 문서에 명시했는가

## G. 범위 대조
- [ ] 견적서의 항목을 하나씩 납품물과 대조했는가 (금액 붙은 항목이 미완이면 무상 수정 사유가 된다)
- [ ] 모바일 실기기에서 핵심 화면 3개를 열어봤는가 (iframe 잘림, 테이블 가로 넘침)

## H. 운영 사건에서 나온 항목
- [ ] CSP 헤더가 결제 PG·분석 스크립트·외부 위젯 도메인을 전부 허용하는가 (막히면 조용히 실패하고 이벤트만 사라진다)
- [ ] 레이트 리밋이 결제 완료 사용자나 관리자 경로까지 막지 않는가
- [ ] 기록 저장(진단·주문·이벤트)이 실패해도 사용자 화면은 정상인 경로가 있는가 -> 있으면 서버 로그로 잡히는지 확인
- [ ] 계좌번호·주민번호·연락처가 외부 AI API로 전송되는 입력 경로가 있는가
- [ ] 도메인 미연결로 IP 직접 접속 상태인가 -> HTTPS 없이 로그인·개인정보가 오가면 실패
